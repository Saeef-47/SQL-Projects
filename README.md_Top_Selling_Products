# Top Selling Products Revenue Analysis

## Project Overview
This project analyzes product sales by calculating total revenue generated by each product using SQL joins and aggregation.  
It helps identify top-selling products based on their revenue contribution.

## Dataset

### Products1 Table

| Column Name  | Data Type       | Description               |
|--------------|-----------------|---------------------------|
| ProductID    | SERIAL (Primary Key) | Unique product identifier  |
| ProductName  | VARCHAR(50)     | Name of the product       |
| Price        | NUMERIC(10, 2)  | Price per unit            |

### Orders1 Table

| Column Name  | Data Type       | Description                       |
|--------------|-----------------|---------------------------------|
| OrderID      | SERIAL (Primary Key) | Unique order identifier          |
| ProductID    | INTEGER         | Foreign key referencing Products1 |
| Quantity     | INTEGER         | Number of units ordered           |
| Sales        | NUMERIC(10, 2)  | Total sales amount for the order  |

## SQL Queries

### Create Products1 Table and Insert Data
```sql
CREATE TABLE Products1 (
    ProductID SERIAL PRIMARY KEY,
    ProductName VARCHAR(50),
    Price NUMERIC(10, 2)
);

INSERT INTO Products1 (ProductName, Price) VALUES
('Apple', 2.50),
('Banana',
